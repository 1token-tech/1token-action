# setup-flyctl (internalized)

内部化自 `superfly/flyctl-actions/setup-flyctl` @ v1.6（commit `ed8efb3383…`），类型 node-js，license MIT。

```yaml
- uses: 1token-tech/1token-action/setup-flyctl@<sha> # setup-flyctl/v1.6
```

Provenance 见 [`../upstreams.yml`](../upstreams.yml)；升级流程见 [`../CONTRIBUTING.md`](../CONTRIBUTING.md)。
运行时仍可能从上游/第三方拉取工具二进制（见 CONTRIBUTING 的内部化边界说明）。
