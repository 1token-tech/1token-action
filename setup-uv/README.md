# setup-uv (internalized)

内部化自 `1token-tech/setup-uv` @ v0.3.0（commit `820443485f…`），类型 composite，license in-house。

```yaml
- uses: 1token-tech/1token-action/setup-uv@<sha> # setup-uv/v0.3.0
```

Provenance 见 [`../upstreams.yml`](../upstreams.yml)；升级流程见 [`../CONTRIBUTING.md`](../CONTRIBUTING.md)。
运行时仍可能从上游/第三方拉取工具二进制（见 CONTRIBUTING 的内部化边界说明）。
