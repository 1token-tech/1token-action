# enable-pull-request-automerge (internalized)

内部化自 `peter-evans/enable-pull-request-automerge` @ v3.0.0（commit `a660677d54…`），类型 composite，license MIT。

```yaml
- uses: 1token-tech/1token-action/enable-pull-request-automerge@<sha> # enable-pull-request-automerge/v3.0.0
```

Provenance 见 [`../upstreams.yml`](../upstreams.yml)；升级流程见 [`../CONTRIBUTING.md`](../CONTRIBUTING.md)。
运行时仍可能从上游/第三方拉取工具二进制（见 CONTRIBUTING 的内部化边界说明）。
