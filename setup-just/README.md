# setup-just (internalized)

内部化自 `extractions/setup-just` @ v4.0.0（commit `53165ef7e7…`），类型 composite，license MIT。

> **已修改**:已改写:nested uses extractions/setup-crate -> 本地 ./setup-crate

```yaml
- uses: 1token-tech/1token-action/setup-just@<sha> # setup-just/v4.0.0
```

Provenance 见 [`../upstreams.yml`](../upstreams.yml)；升级流程见 [`../CONTRIBUTING.md`](../CONTRIBUTING.md)。
运行时仍可能从上游/第三方拉取工具二进制（见 CONTRIBUTING 的内部化边界说明）。
